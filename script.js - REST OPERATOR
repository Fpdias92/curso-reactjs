// REST OPERATOR

function minhaLista(...nomes){   // Exibe todos os itens que forem inseridos na função, em vez de mostrar apenas o primeiro nome
    console.log(nomes);
}

minhaLista("Felipe", "Augusto");


// [nomes] = Felipe
// [...nomes] = [Felipe, Augusto]